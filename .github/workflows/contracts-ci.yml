name: Contracts CI
on:
  push:
    paths: ['contracts/**', '.github/workflows/contracts-ci.yml']
  pull_request:
    paths: ['contracts/**', '.github/workflows/contracts-ci.yml']

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
